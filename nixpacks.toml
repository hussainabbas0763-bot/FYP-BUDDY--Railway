# Python virtual environment setup for chatbot dependencies
[phases.setup]
nixPkgs = ["nodejs_22", "python311", "python311Packages.pip", "python311Packages.virtualenv", "gcc-unwrapped.lib"]

[phases.install]
cmds = [
  "npm ci",
  "npm install --prefix frontend --legacy-peer-deps",
  "python -m venv /opt/venv"
]

[phases.build]
cmds = [
  "npm run build --prefix frontend",
  ". /opt/venv/bin/activate && pip install -r backend/Chatbot/requirements.txt"
]

[start]
cmd = "export LD_LIBRARY_PATH=/nix/store/*-gcc-*-lib/lib:$LD_LIBRARY_PATH && . /opt/venv/bin/activate && npm run start"
